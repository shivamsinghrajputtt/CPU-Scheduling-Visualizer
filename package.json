{
  "name": "cpu-scheduling-visualizer",
  "version": "2.0.0",
  "private": true,
  "description": "Interactive CPU scheduling simulator and visualizer",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
